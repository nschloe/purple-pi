name: ci

on: [pull_request,push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@1
      with:
        node-version: "10.x"
    - run: npm install
    - run: npm run lint
