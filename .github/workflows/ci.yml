name: ci

on: [pull_request,push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node
      with:
        python-version: "3.x"
    - uses: actions/checkout@v2
    - run: npm install
    - run: npm run lint
